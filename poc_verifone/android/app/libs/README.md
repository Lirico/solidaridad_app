# Verifone PaymentSDK-SDI binaries

| File | Role |
|------|------|
| `PaymentSDK-4.1.0-sdi.aar` | **Active dependency** — PSDK-SDI 4.1.0 (`com.verifone.sdi.payment_sdk`). Printer / MSR via `SdiManager`. |
| `PaymentSDK-4.1.0-sdi-javadoc.jar` | API docs (not linked by Gradle). |

From 4.0.1 the Android package was renamed from `com.verifone.payment_sdk` to `com.verifone.sdi.payment_sdk`.

Do not commit the large symbols zip from non-SDI portal downloads.
